start angular server:   ng serve

start node server: npm run start:server
  //
    Using nodemon.
    It requieres defining the script in the package.json file.
      Example:
        "start:server": "nodemon server.js"
    also requires adding
      const debug = require("debug")("node-angular");
    to the server.js file.
    "node-angular" is a user-defined identifier
  //

