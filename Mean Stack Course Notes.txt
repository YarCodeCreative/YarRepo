start angular server:   ng serve

start node server: npm run start:server
  //
    Using nodemon.
    It requieres defining the script in the package.json file.
      Example:
        "start:server": "nodemon server.js"
    also requires adding
      const debug = require("debug")("node-angular");
    to the server.js file.
    "node-angular" is a user-defined identifier
  //



Section 4 - Working with MongoDB

- needed to create a free account on MongoDB.com
  - Create a new project, then a new cluster
  - Create a new user with a new password
  - Add local IP to IP Whitelist under Network Security

- Install mongoose package using npm
    3rd party package that allows for schemas for storing data.
    - install by terminal:
        npm instal --save mongoose
    - mongoose will create ID's on its own
